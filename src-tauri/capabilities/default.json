{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Denki main window — the app is fully local-first (IndexedDB) and needs no privileged APIs.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
